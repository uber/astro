---

session_repo_dir: /tmp/terraform-tests/plan-detach

modules:
  - name: foo
    path: .
    remote:
      backend: local
      backend_config:
        path: /tmp/terraform-tests/plan-detach/local-backend/foo.tfstate
